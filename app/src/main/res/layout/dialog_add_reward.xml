<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <ImageView
            android:id="@+id/addRewardImagePreview"
            android:layout_width="120dp"
            android:layout_height="120dp"
            android:layout_gravity="center_horizontal"
            android:contentDescription="Vista previa del premio"
            android:scaleType="centerCrop"
            android:src="@drawable/reward_image_placeholder" />

        <Button
            android:id="@+id/choosePhotoButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:background="@drawable/button_soft_action"
            android:text="Subir foto"
            android:textAllCaps="false"
            android:textColor="@color/ink" />

        <Button
            android:id="@+id/clearPhotoButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:background="@drawable/button_soft_action"
            android:text="Quitar foto"
            android:textAllCaps="false"
            android:textColor="@color/ink" />

        <EditText
            android:id="@+id/rewardDescriptionInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:hint="Descripción del premio"
            android:inputType="textCapSentences"
            android:padding="12dp" />

        <EditText
            android:id="@+id/rewardPointsInput"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:hint="Puntos"
            android:inputType="number"
            android:padding="12dp" />
    </LinearLayout>
</ScrollView>
